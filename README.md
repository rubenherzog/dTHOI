# dTHOI

dTHOI is a high-throughput library for higher-order information analysis of discrete multivariate data.

The initial core supports binary observations, exact count-based entropy estimation, entropy memoization, TC/DTC/O/S computation, and lazy exhaustive multi-order analysis. Heuristic search and performance optimizations are intentionally deferred until the core is validated.
